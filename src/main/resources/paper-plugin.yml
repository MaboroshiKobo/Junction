name: Junction
version: "${version}"
main: org.maboroshi.junction.Junction
loader: org.maboroshi.junction.Loader
description: Automate permissions and execute edition-specific commands for Java and Bedrock players using Floodgate.
author: Maboroshi
website: https://github.com/MaboroshiKobo/Junction
api-version: "26.1"
folia-supported: true
dependencies:
  server:
    floodgate:
      load: BEFORE
    LuckPerms:
      load: BEFORE
      required: false
    Vault:
      load: BEFORE
      required: false
    PlaceholderAPI:
      load: BEFORE
      required: false
